apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finagent-auth-service-test
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://git.example.com/your-org/finagent-gitops.git
    targetRevision: HEAD
    path: finagent/apps/test/finagent-auth-service
    helm:
      valueFiles:
        - values.yaml
        - values-test.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: finagent-apps-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
