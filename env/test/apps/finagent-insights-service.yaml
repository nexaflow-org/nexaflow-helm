apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finagent-insights-service-test
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://github.com/FinAgent-ORG/finagent-helm.git
    targetRevision: main
    path: apps/test/finagent-insights-service
    helm:
      valueFiles:
        - values.yaml
        - values-test.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: finagent-apps-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
