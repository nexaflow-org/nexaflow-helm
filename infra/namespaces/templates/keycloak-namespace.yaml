apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespaces.keycloak }}
  labels:
    {{- include "finagent-namespaces.labels" . | nindent 4 }}
    finagent.io/scope: keycloak
    shared-gateway: "true"
