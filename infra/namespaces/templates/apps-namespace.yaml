apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespaces.apps }}
  labels:
    {{- include "finagent-namespaces.labels" . | nindent 4 }}
    finagent.io/scope: apps
    shared-gateway: "true"
